package lms;

class Book {
    String title;
    boolean available;
    float price;
    Book(String title, float price) throws IllegalArgumentException {
        this.title = title;
        this.available = true;
        if(price<0)
        	throw new IllegalArgumentException("Price cannot be negative");
        this.price = price;
    }
}

